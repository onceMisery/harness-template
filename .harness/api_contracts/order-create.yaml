name: CreateOrder
method: POST
path: /api/orders
request:
  body:
    user_id: string
    items:
      - sku_id: string
        quantity: integer
    address_id: string
response:
  200:
    order_id: string
    status: string
    amount: number
  400:
    code: INVALID_ARGUMENT
    message: string
